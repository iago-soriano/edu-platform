// import { toThrowFieldError } from './../../../test/jest/matchers/custom-error';
import { Content } from ".";
import { DomainRules } from "@edu-platform/common";
import "../../../test/jest/matchers/custom-error";
import { expect } from "@jest/globals";

describe("Unit tests for Image Content domain entity", () => {
  // testar se funções de upload e delete são chamadas, em cada caso: this.originatingVersionId !== versionId com newContent.file e !newContent.file, e this.originatingVersionId === versionId
  // com newContent.file e !newContent.file. Colocar valores diferentes no newContent e ver se são atualizados corretamente
  it.todo("", () => {});
});
