export const NameCell = (info) => (
  <p className="text-center">{info.getValue()}</p>
);

export const LanguageCell = (info) => (
  <p className="text-center">{info.getValue()}</p>
);

export const LevelCell = (info) => (
  <p className="text-center">{info.getValue()}</p>
);

export const TypeCell = (info) => (
  <p className="text-center">{info.getValue()}</p>
);

export const TopicsCell = (info) => (
  <p className="text-center">{info.getValue()}</p>
);
