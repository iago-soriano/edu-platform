{
    "version": "0.2.0",
    "configurations": [
      {
        "command": "cd src/api && npm run dev", 
        "name": "Run npm dev", 
        "request": "launch", 
        "type": "node-terminal",
        "outFiles":["${workspaceFolder}/src/api/build/**/*.js"],
        "resolveSourceMapLocations": [
            "${workspaceFolder}/**",
            "!**/node_modules/**"
          ],
      }
    ]
  }